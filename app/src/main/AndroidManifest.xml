<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ashrlm.cardsagainstsociety">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/API_KEY" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".credits" />
        <activity
            android:name=".editDecks"
            android:label="@string/title_activity_edit_decks" />
        <activity
            android:name=".editDeck"
            android:label="@string/title_activity_edit_deck" />
        <activity
            android:name=".newDeck"
            android:label="@string/title_activity_new_deck" />
        <activity
            android:name=".setupNewGame"
            android:label="@string/title_activity_setup_new_game" />
        <activity
            android:name=".gameLobby"
            android:label="@string/title_activity_game_lobby"></activity>
    </application>

</manifest>